-- Script de inicialização do banco de dados
-- Este arquivo é executado automaticamente quando o container PostgreSQL é criado

-- Verificar se o banco existe (já é criado pelo docker-compose)
SELECT 1;